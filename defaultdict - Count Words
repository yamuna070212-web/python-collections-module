from collections import defaultdict

sentence = "python java python c java python"

count = defaultdict(int)

for word in sentence.split():
    count[word] += 1

print(count)
Output
defaultdict(<class 'int'>, {'python': 3, 'java': 2, 'c': 1})
